// Importing this module runs the host. Exists so `import '@aipehub/host'`
// works the same as running the bin. For programmatic embedding, use the
// underlying packages (@aipehub/core, @aipehub/web, @aipehub/transport-ws)
// directly instead — they are the supported public API.
import './main.js'
