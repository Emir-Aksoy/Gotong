// Importing this module runs the host. Exists so `import '@gotong/host'`
// works the same as running the bin. For programmatic embedding, use the
// underlying packages (@gotong/core, @gotong/web, @gotong/transport-ws)
// directly instead — they are the supported public API.
import './main.js'
