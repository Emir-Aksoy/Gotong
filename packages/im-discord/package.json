{
  "name": "@gotong/im-discord",
  "version": "0.1.0",
  "type": "module",
  "description": "Gotong IM bridge for Discord — implements ImBridge against the Bot API (WebSocket Gateway v10 + REST). No discord.js dep (uses fetch + WebSocket).",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Emir-Aksoy/Gotong.git",
    "directory": "packages/im-discord"
  },
  "publishConfig": {
    "access": "public"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepack": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@gotong/im-adapter": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
