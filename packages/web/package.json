{
  "name": "@gotong/web",
  "version": "4.0.0",
  "type": "module",
  "description": "Gotong reference web UI — embeddable HTTP + SSE server with a vanilla-JS frontend.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Emir-Aksoy/Gotong.git",
    "directory": "packages/web"
  },
  "publishConfig": {
    "access": "public"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src",
    "scripts",
    "static",
    "README.md"
  ],
  "scripts": {
    "build": "node scripts/build-admin-ui.mjs && node scripts/build-static-assets.mjs && node scripts/build-builtin-templates.mjs && tsc -p tsconfig.json",
    "build:admin": "node scripts/build-admin-ui.mjs",
    "build:assets": "node scripts/build-admin-ui.mjs && node scripts/build-static-assets.mjs",
    "build:templates": "node scripts/build-builtin-templates.mjs",
    "build:site": "node scripts/build-site.mjs",
    "build:leaderboard": "node scripts/build-leaderboard-doc.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepack": "node scripts/build-admin-ui.mjs && node scripts/build-static-assets.mjs && node scripts/build-builtin-templates.mjs && tsc -p tsconfig.json"
  },
  "dependencies": {
    "@gotong/core": "workspace:*",
    "@gotong/protocol": "workspace:*",
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "@gotong/identity": "workspace:*",
    "@gotong/workflow": "workspace:*",
    "esbuild": "^0.28.1",
    "vitest": "^4.1.10"
  }
}
