{
  "name": "@aipehub/web",
  "version": "3.0.0",
  "type": "module",
  "description": "AipeHub reference web UI — embeddable HTTP + SSE server with a vanilla-JS frontend.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Emir-Aksoy/AipeHub.git",
    "directory": "packages/web"
  },
  "publishConfig": {
    "access": "public"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src",
    "scripts",
    "static",
    "README.md"
  ],
  "scripts": {
    "build": "node scripts/build-static-assets.mjs && tsc -p tsconfig.json",
    "build:assets": "node scripts/build-static-assets.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepack": "node scripts/build-static-assets.mjs && tsc -p tsconfig.json"
  },
  "dependencies": {
    "@aipehub/core": "workspace:*",
    "@aipehub/protocol": "workspace:*",
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
