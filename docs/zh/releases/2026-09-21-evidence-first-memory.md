# 2026-09-21 证据优先记忆发布与部署

方向：M；发布与部署属维护。开发分支 `codex/memory-evidence-retrieval`，基线 `ae84532c`。

## 已交付

| 提交 | 内容 |
|---|---|
| `8d05d970` | 原始证据/时间呈现，默认有效性，筛选先于 top-k，历史查询 |
| `1c498f11` | 完整扫描及导出，降温与事实失效分离 |
| `516d53a0` | 有预算的跨库搜索和版本核验读取，可信用户原话写入，审批恢复保留原时间 |

行为与限制见 [ATONG-MEMORY-RETRIEVAL.md](../ATONG-MEMORY-RETRIEVAL.md)，实施清单见 [ATONG-MEMORY-UPGRADE-PLAN.md](../ATONG-MEMORY-UPGRADE-PLAN.md)。

## 验证

- `pnpm --filter @gotong/host... build` 通过；最后的预算选配调整另经 personal-memory 构建通过。
- `pnpm -r typecheck` 全工作区通过。
- personal-memory：763 项通过。
- personal-butler：428 项通过。
- service-memory-file：191 项通过。
- host：3761 项通过、5 项原有跳过；共 5143 项通过、5 项跳过。
- `pnpm check:guards` 四门通过，环境变量仍为 114 个，热点文件预算不变。
- 记忆 recall/write/integration/eviction 四项专门验收通过。
- `git diff --check` 通过。

新回归覆盖：失效种子挤掉有效记录、来源时间丢失、关联筛选绕过、500 条以外去重/预算/导出、缺全量能力拒绝维护、降温仍保持事实有效、旧来源复活、模型伪造确认、任务共享字节预算、Unicode 片段、陈旧版本拒绝、fresh 读取更新搜索缓存、审批后重建 agent 仍保留原话时间与成员绑定。

独立只读代码审查发现两项 P2：fresh 读取未更新搜索缓存、审批恢复丢失当前用户证据。均补失败回归后修复并复核关闭；审查不是对整个项目的全面安全背书。

验证过程如实记录：初次 host 全量运行被沙箱禁止本机监听（`listen EPERM`），允许本机测试端口后重跑；升级引起的旧 list 拦截和假后端缺 scan 已调整为等价行为测试。并发构建/类型检查期间另遇交换状态短暂为 running，以及家庭工作流测试清理目录 `ENOTEMPTY` 的时序失败；未修改这些无关业务代码，相关定向测试和最终无并行构建的完整 host 回归通过。

## GitHub 与服务器交付

- 用户明确授权推送和部署后，四项本地提交（含 `64aba60e` 收口文档）已快进合并并推送 GitHub/main，远端由 `ae84532c` 前进至 `64aba60e`。没有强推或操作其他远端分支。
- 推送前重新构建，并复跑 personal-memory 763、personal-butler 428、file 191、host 3761 项：共 **5143 项通过、5 项原有跳过**。整仓类型检查、框架四门和四项记忆验收也在本轮重新通过。
- 推送后 Git 传输端点的额外回读遇到 HTTP/2 错误和超时；GitHub API 独立回读确认 main 为 `64aba60e4a4e3d6b3520d51c1e23d6fe4074c4c2`。GitHub 仍提示 22 项依赖告警（16 high、6 moderate），本轮没有升级依赖或宣称修复这些告警。
- 服务器于 **2026-09-21T19:05:08+08:00** 完成部署，运行源码由 `db5bb875` 快进至 **`64aba60e`**，部署后工作树干净。本次后续发布记录提交仅改文档，不改变服务器运行代码。
- 沿用增量 Git bundle 流程；依赖清单、锁文件和 identity 代码无变动，沿用已有依赖并串行构建成功。只保存受限访问的旧构建用于失败回退，不新增用户记忆备份。停止至完成检查约 2 分 11 秒。
- 服务器现场回归：personal-memory 763、personal-butler 428、file 191、host 记忆集成 67 项，共 **1449 项通过**。测试使用合成数据，不调用生产模型或用户记忆。
- 编译产物直接验证了共享 6000 字节预算、2000 字节单页限制、嵌套作用域和重复读取约束；旧时间冒烟仍通过，服务器时区为 `Asia/Shanghai`。
- `/healthz`、`/readyz`、首页及 8 个首页本地静态资源全部返回 200。Gotong 和看门狗正常运行，检查时 `NRestarts=0`；同机其他既有应用与反向代理进程未更换。
- 重启后结构化日志采样为 0 error / 1 warn，未将该 warning 宣称为已修复。部署脚本、旧构建和详细日志留在受限运维目录，连接信息与私有路径不进入公开记录。

## 发布边界

**已推送 GitHub/main，已部署服务器**。没有读取或修改生产凭据，没有执行真实记忆的迁移、回填、纠正或硬删除，也未恢复暂停的入口。

本地忽略的 `CLAUDE.md` 已加导航指针，未强制纳入 Git。旧事实不回填，旧 `validTo` 不自动修正。线上真实模型对记忆工具的使用效果尚未验证。
