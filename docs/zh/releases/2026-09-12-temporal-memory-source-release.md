# 时间记忆阶段收口：源码交付

方向: M。用户于 2026-09-12 要求停止继续扩展，立即整理并推送已进行的改动。

## 推送结果

- 实现及收口提交 `6def862d` 已正常推送至 GitHub/main，远端从 `0b92bce1` 快进；没有强推。本次文档补记不改变运行代码。
- 推送前再次运行宿主测试：3759 项通过、5 项原有跳过；框架门通过。待推送提交的新增行检查未发现常见凭据模式，此检查不是完整秘密审计。
- GitHub 推送回执提示 22 项依赖安全告警（16 high、6 moderate）。本次不修改依赖或锁文件，也不将这些告警宣称为已解决。
- **服务器未部署**。未执行生产重启、真实记忆变更、回填或用户硬删除。

## 交付边界

- 交付对象为 GitHub `Emir-Aksoy/Gotong` 的 `main`，仅正常快进推送，不强推、不改依赖机器人分支。
- 已完成改动截至 `2a9250de`；基础时间链 `9c47abfb`，来源保真 `c6bcb1e3` / `ee977dbe`，日期词锚定 `60aa9151`。
- 本轮不部署服务器、不调用生产模型、不读取或修改真实用户记忆，不进行历史数据回填。
- 已提交的内部安全和纠正底座一并保留，不为推送新增功能，也不重写既有提交历史。

## 已有能力

- 新任务按服务器时区固定原时间锚，近期对话与记忆呈现携带时间标签；后续时钟变化不重新解释原锚。
- 整理保留用户原话、来源及时间依据；助手回复不能直接成为用户长期事实。新时间证据不只按文本相似折叠。
- 有界相对日期词按原消息时间解释，不把记录时间冒充事件时间，不给旧数据补日期。
- 文件纠正、索引退役、共享排空、持久隔离和 Git 历史清理已有内部实现；没有启用完整用户纠正/硬删除入口。

## 明确不承诺

- 日期词解释不等于可靠完成所有事件关联；没有真实模型“上上周烤肉”问答准确率验收。
- 没有实现完整跨区块 token 预算或事件/告知时间检索；现有上下文上限未扩大。
- `forget`/`forgetAll` 不等于所有受管副本彻底清除。投影、会话与完整恢复协调仍有缺口，相关入口保持关闭。
- Git 清理不证明外部反向引用无影响，也不覆盖外部备份或物理介质擦除。

## 验证依据

- 截至 `88371bc8` 的实现已完成 file 190 / personal-memory 753 / personal-butler 418 / host 3759，合计 5120 项通过、5 项原有跳过。
- 全仓类型检查、host 依赖构建、框架门及四道记忆门通过；推送前再次执行宿主完整回归和框架门。
- 前一轮曾发生既有 WorkBuddy Python 子测试偶发阻塞，单独与完整重跑通过；未将其标为已修复。完整证据见专题的逐阶段验证记录。

## 后续纪律

本次收口后停止继续扩展硬删除底座。“尚未完成”用于如实界定发布能力，不再自动变成一串开发前置任务。
部署和真实场景验收另按用户指令安排；不能把 GitHub 已推送等同于服务器已升级。

详细实现和历史阶段记录见 [`ATONG-TEMPORAL-MEMORY.md`](../ATONG-TEMPORAL-MEMORY.md)。
