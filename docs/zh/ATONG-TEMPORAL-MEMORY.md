# Atong 时间记忆

方向: M。M1 与 M2 的来源保真、原话事实整理已本地实现并验证，未发布、未部署。
M2 新增有界日期词解释、纠正底座、内部文件提交、检索缓存退役、用户活动排空及持久化准入隔离；用户纠正入口、完整硬删除协调与 M3 仍待实现，不能把阶段收口理解为整个时间记忆计划完成。

## 时间口径

采用服务器时区，不猜测用户所在地。每条新对话固定保存当时解析出的 IANA 时区；
无法解析时明确保存 UTC。服务器以后更换时区，不会重新解释已有时间锚。

- `meta.temporal.observedAt`：本地 agent 开始处理新任务的时间，不是外部渠道原始发送时间。
- `meta.temporal.timeZone`：这次新任务观察时的服务器时区。
- `meta.temporal.basis = turn-start`：明确说明上述依据。
- `MemoryEntry.ts`：记忆写入时间，召回时明确标为 `recorded`。
- 事件发生时间：M1 不提取、不推断，显示 `event-time: unspecified`；不能把告知日当成发生日。

会话窗的新消息也保存服务器时区，并在送给模型的历史正文前显示时间标签。
标签与正文一起计入原有长度限制；同角色消息合并时保留各条原有标签。

## 已实现边界

1. 在模型工作开始前固定时间，长回复跨午夜不改变原任务时间锚。
2. 静态附加 metadata 与任务 payload 不能冒充受信任的本地时钟。
3. 没有持久化原时间锚的恢复任务不编造原消息时间；底层恢复失败而重跑时只捕获一次。
4. 带新时间锚的轮次不按字面相似度折叠。无锚调用也不能折入带锚记录；旧无锚路径保留原新颖门。
5. 冻结记忆块显示时间标签，标签计入既有正文预算。放不下时整条省略，不裁掉日期后继续冒充完整证据。
6. 不运行历史数据迁移，不恢复已删除或丢失的记忆，不给旧条目补日期。

**取舍**：新对话暂时会保留更多独立记录。原有存储预算和逐出机制仍有效，
这并非降低了存储量；旧新颖门基准的节省数字不能作为新时间链路的成绩。
冻结块与会话窗限长未提高；recall 的跨模块统一 token 预算仍待 M3。

## 尚未完成

- **M2 后续**：将日期词与具体事件可靠关联、明确纠正关系及复杂转述中的时间参照。
  已实现的日期词解释不等于自动确认事件发生日期。
- **硬删除收尾**：文件层已有版本核对、旧句柄屏障与失败恢复，检索层已有实例退役/缓存清理；
  router 与逐用户维护已有共享的进程内排空及重启准入隔离；仍需覆盖未登记的访问、投影/Git 清理、
  存活及暂停会话上下文退役，以及可信用户纠正意图接线。
  当前 `forget`/`forgetAll` 不能被当成完整链路。
- **M3**：按事件时间/告知时间检索、历史问题召回，跨提示词区块按来源去重并统一固定 token 预算。

因此，M1 证明的是时间记录与呈现链路，不是“所有日期问题都能正确回答”。
未调用真实模型评测，也未读取用户历史对话；测试用合成文本与注入时钟。

## M2 来源保真

新对话在捕获时记录用户正文的精确位置，不靠解析正文里的 `User:` / `Butler:` 标签。
保留用户原文的换行和缩进，避免把分行日期与事件压平后改变对应关系。
这是**对话角色来源**，不是新增的身份认证机制；成员与机构隔离仍由宿主的既有授权边界负责。
截断过的用户正文不会作为完整事实晋升，旧记录缺少位置证据时不推测补齐。

普通整理、分层整理、摘要晋升遇到新证据时采用保留原话的路径：

- 派生正文只保存用户原话；元数据记录原始来源 ID、原话位置与原时间锚，不重复保存整份原话。
- 一条派生记忆至多含 8 个原始来源。正文和元数据一起计入既有整理上限；超限留源，不截断证据。
- 写入返回的记录须通过证据一致性检查，之后才删除已完整保留的来源。低重要度不是跳过此检查的理由。
- 同一来源 ID 若出现不同内容、时间或成员作用域，不能静默选择其中一个。
- `userId` / `user` 两个既有作用域字段若矛盾或类型错误，拒绝读取、打包及写后验证。
- 冻结块与 recall 显示原始引用和原时间，不把新摘要的写入时间当成事件时间。
- 旧自由改写式 reconcile 不能更新或删除这些证据；用户显式遗忘与既有存储保留政策不受影响。

来源 ID 指向**原始来源身份**；原始记录整理后可能已删除，派生记录保留的是原话证据，
不代表原记录仍可按 ID 读取。没有新增读取其它空间或备份的能力。

原子事实整理也改为由模型选择来源 ID，而不是接受模型自由生成的事实正文。
实际保存的是选中来源的完整用户原话。即使模型误选了“上周买过一杯奶茶”，
也不能把它写成“用户最爱的饮料是奶茶”。缺少结构化来源的旧混合对话不进入这条自动提炼链。
来源编号相同必须再核对原文、时间和归属；冲突在写入前报告给现有维护健康链，不覆盖旧记录。

**取舍**：这是保守的原话整理，不是任意自然语言语义压缩。会减少助手回复和重复原文的占用，
但并不保证每条短记忆都更小；仍保留旧存储预算，未扩大提示词上限，也没有宣称新的同义词召回提升。
新证据暂用确定性关键词分类与已有重要度，不再由模型在改写摘要时推断分类和评分；
旧无结构记录的模型整理路径仍保留。来源保真与未来更精准的分类是两个不同的验收项。

## M2 日期词解释

新捕获通过 `meta.calendar` 保存版本化的原话位置和本地日历范围，整理时随每个来源保留。
支持：今天、昨天、前天、明天、后天；本周、这周、上周、上上周、下周、下下周；
明确的 `YYYY-MM-DD` / `YYYY年M月D日`。不使用模型、外部服务或新的配置项。

- 相对日期锚定原任务的服务器本地日期，周按周一开始、下周一结束的半开区间保存。
  例如服务器日期 2026-09-11 时的“上上周”为 `[2026-08-24, 2026-08-31)`，即 8 月 24 日至 30 日。
- 日历日期不是 UTC 时刻，未编造每天几点发生；跨午夜、闰日、跨年、夏令时不靠本地 24 小时加减。
- 缺少可信时间锚的相对词保持未知，非法日期不滚入下个月；缺年日期、上周三/周末等未支持表达不擅自补精度。
- “去年今天”“上周的今天”等已知复合参照，以及“后天性疾病”等非日期词保守跳过，避免截取其中两个字误算。
- 每段原话最多四个日期词，超出明确标注省略；正文、日期和来源元数据继续计入既有整理上限。
- 读取只验证已有版本和原时间锚，不给旧条目生成日期字段。后来的系统时间和进程时区不参与重新锚定。
- 召回和冻结上下文显示日期解释，避免重复输出同一告知时间；放不下仍整条省略，不扩大预算。

**重要边界**：这是“日期词按原告知日解释”的记录，不是自然语言事件解析器。
问句、否定、假设、引文、转述仍保留原话，并标明 `not event claims`；不得仅凭这个范围断言事情发生。
例如“昨天他说上周”的内部参照或“上周左右”的模糊事件范围尚未求解，不能把字面日期范围当最终事件日期。
没有自动关联两条相似陈述，也没有通过新记忆覆盖旧事件。真实模型答对日期的比例需要后续独立评测。

## M2 纠正底座

**用户确认的行为**：明确纠正且旧来源唯一时直接替换，不额外确认；被纠正内容及相关受管副本硬删除，
不保留旧版本。只在目标含糊时询问，不因描述相似而删记忆。不进行历史批量迁移、补写或恢复。

本阶段只实现两项底层能力，**尚未接入对话或删除任何真实记忆**：

1. 文件后端 `snapshot()` 在同一用户的进程内写队列中完整读取已配置种类，不受交互列表 500 条上限影响。
   原字节指纹用于后续写入前核对；损坏内容、非普通文件、越界链接和读取错误明确失败，不返回部分结果。
   `list` / `recall` 上限未改变，旧预算和导出调用也尚未改用快照，不能据此宣称它们的全量问题已经修复。
2. `prepareEvidenceCorrection()` 只计算结构化来源的变更：精确完整原话匹配，同一来源多份副本一并定位，
   不同来源同一句话须消歧。混合容器仅重打包其它来源的原文、时间和日历信息，不继承旧聚合元数据；
   新用户陈述单独保存为 semantic 候选，使用本次纠正的时间锚和新的来源 ID。

来源冲突、坏证据和 packed/raw 双表示不静默裁决；合法但没有用户来源的助手记录与旧无结构记录列为
`untraced`，不能凭字符串相似就删除。计算器不识别任意自然语言纠正意图，也不承担成员鉴权。
输入必须来自完整、已授权的单用户快照；返回 `ready` **仅表示可计算，不表示已授权、已落盘或已彻底删除**。
字节上限仍生效，超限整次拒绝，不截断无关来源以腾位置。
重建条目补齐原编号和写入时间后复验完整字节数；新条目仍是待保存载荷，执行器生成编号/写入时间后还须核预算。

完整执行器必须在落盘前重核快照并处理未追踪派生内容、索引、Git 历史和会话缓存，不能让旧维护任务重新写回。
完整快照也不是跨进程事务；应用外部备份和操作系统快照不属于可承诺清除的范围，历史备份目录不触碰。

## M2 内部文件提交

`MemoryFileHandle.applySnapshotMutation()` 在同一 owner 队列内重核完整快照，批量删除、重建和追加。
快照指纹绑定 owner、配置种类、文件原字节与持久化代际。目标不存在、重复编号、过期版本、非法载荷或
完整条目/种类文件超限均在修改主文件前拒绝；不截断无关记忆来腾出空间，未修改的行保留原字节。

- 恢复记录只含修改后内容及前后指纹，不建立旧文件备份。先同步并发布恢复记录，再原子替换各主文件。
- 多文件替换不是文件系统级原子事务：中断后阻止受管句柄普通读写，可信宿主显式调用 `recoverMutation()`。
  恢复先核验所有受影响文件均处于预期的修改前/后状态，再统一向前完成；未知漂移拒绝覆盖，不回滚旧原文。
- 临时后像按事务代际及固定种类命名，恢复前检查并清理已验证残留。未知归属、损坏或部分写入的临时文件
  保守阻断，不通配删除。清理完成并同步后，最后解除 pending 标记；此后没有可能返回失败的文件操作。
  标记删除不承诺掉电持久化，重启可能再次要求恢复；代际已是修改后时，只有全部后像匹配才接受，防止覆盖后续写入。
- 正常读写过的句柄固定所属代际；纠正成功后，旧句柄的读写均失败，不静默重开。
  成功提交的句柄、新建句柄，以及完成显式恢复的可信调用方可使用新代际。
- 新增内部 `correctButlerMemoryFiles()` 自行按可信 userId 开库、计算方案、拒绝未追踪记录并提交。
  输入在异步读取前复制，调用方附带的 userId 不能覆盖宿主作用域；结果只叫 `memory_files_updated`。

**尚未连接 Web、对话或模型工具，也没有对用户真实记忆执行纠正。** 文件屏障不覆盖直接读取 JSONL 的索引、
已经构建的提示词或第三方进程。下一阶段必须在宿主按用户隔离期间清理派生内容、退役旧执行，完成后才能开放入口：

- recall-index 的内存及磁盘副本、进行中的刷新，以及 memory-net 的 TTL/失败回退缓存。
- 维护、Obsidian 投影、DREAMS/SKILL/STATUS 和 Git 快照；Git 还须明确排除事务内部文件。
- router 的用户实例、记忆服务的旧句柄、IM/Web 会话窗、暂停任务及迟到回复。
  群聊窗口涉及其他成员，不能按单用户目录直接粗暴清空。

不新增跨进程锁，不承诺物理介质安全擦除，不触碰应用外部备份；完整协调期间的隔离不能随文件 journal 消失就解除。

## M2 检索缓存退役

索引不再直接宽松读取 JSONL，而是通过文件句柄的严格全量快照重建；损坏记录不静默跳过。
热路径使用 `watermark()`，绑定 owner、种类、代际与 dev/ino/size/mtimeNs/ctimeNs，只读文件属性及很小的
代际标记，不每轮读取整份 JSONL。它是缓存新鲜度标记，**不是内容指纹或修改授权**，文件提交仍用完整快照 CAS。

- `assertUsable()` 检查 pending、旧代际及实例退役状态，warm cache 命中也不能跳过；异步检索返回前再次检查。
- `retire()` 立即封闭旧索引并清空内存，等待已开始的刷新/持久化结束后，严格删除 `recall-index.json`
  及已知格式的原子写临时文件。清理前完整预检，符号链接和未知相似文件拒绝，不触碰其他用户或无关文件。
- 清理失败不冒充成功，可重试但不会重新启用旧对象。`clear()` 仍只是非破坏性内存清理，不等于硬删除。
  清理后发起的读取若加入旧刷新，须继续等待当前代际完成发布，不把旧轮放弃刷新误当成空记忆。
- 联想网 TTL 命中、重建完成和失败回退都检查索引可用性；屏障失败清掉旧网，晚到的建网结果也不能复活它。
  已向上传播的访问错误不能因下一次检查恢复正常就降级为普通回退，必须先清空旧缓存。
  正常情况下保留 TTL/并发合流；辅助库偶发失败仅在记忆仍可用时回退旧网。不新增模型调用、环境变量或上下文容量。

**这仍是实例级能力，不是已经完成的用户级硬删除。** 多个 agent 可能持有同一用户的不同索引，最终协调器须
先阻止新建并退役全部相关实例，再修改源文件。新的实例不能在清理期间启动；已经交给模型的上下文无法靠
索引检查撤回。Git/投影、活跃/暂停任务、会话窗与可信纠正意图仍待接线，本轮不调用真实用户的 retire 或纠正。
文件路径检查针对静态越界，不承诺抵抗其他进程恶意替换文件；磁盘缓存可重建，清理不包含外部备份。
与退役重叠且已通过检查的在途读取可能在退役完成前交付；不能撤回已交付结果。退役完成后旧实例拒绝新读取。

### 协调接入顺序

1. 用户级隔离与排空：多个 router 和逐用户后台维护已登记，持久标记覆盖其重启准入；下一步覆盖仍在登记外的入口。
   `ButlerMaintenanceSweeper.stop()` 仍只停止定时器，不代替 `quiesce()`；停止整个 Hub 也不代替可恢复的用户级协调。
2. 受管副本清理：隔离期间退役全部索引，清理 Git 历史、派生 Markdown/投影，以及包含旧上下文的会话和暂停任务。
   Git 必须验证本用户自有目录，拒绝链接到其他仓库；群聊不得粗暴删除其他成员的数据。
3. 可恢复完成：隔离跨越文件事务的完成点，失败和重启后继续拒绝普通使用；全部清理成功才允许新实例打开新代际。
4. 最后连接可信纠正入口；目标含糊时消歧，唯一明确纠正才执行。此前不宣称“已全部删除”。M3 的检索与预算另行推进。

## M2 用户活动排空

本阶段接 `ButlerUserActivity`：main 为同一记忆根目录创建一个登记器，共享给所有阿同 router 和维护扫描器。
`run()` 在工作调用之前登记，并等待完整 Promise，包括回复后的记忆捕获；`quiesce()` 立即拒绝同用户新工作，
等已有工作真实结束，再串行退役资源。迟到的返回值、异常和暂停状态统一变成不含原文的固定错误。
成功退役的资源不重复处理，失败的保留重试；挂住时继续隔离，取消通知或超时不等于执行已停止。

- router 的任务、恢复、取消和正常关闭回调，以及每用户后台整理、后续 Git 快照和无模型投影，参与同一登记。
  按需整理已在任务内登记，不再嵌套入门。其他用户不会被一起关闭。
- 正常关闭只封闭本 router，等待其在途工作后关闭实例、注销资源并移出缓存；已被用户隔离接管的资源不能注销。
  成功 shutdown 不重复，失败保留重试。轻量磁盘清理回调单独创建，不保留 agent/provider/index 引用，每用户只登记一次。
- 资源退休与共享缓存收尾分为两阶段：全部资源成功后才运行 `registerFinalizer()` 登记的清理。
  资源失败时不消费收尾责任，防止 shutdown 重试再造缓存后，已完成的索引退役不再删除新副本。
- 管理器的 stop 请求不是排空完成通知：实际移除时须同步封闭旧 router，再异步排空，避免停止自身的工具自等。
  既有 service/MCP detach 行为不改；纠正协调器仍必须等待用户级 `quiesce()`，不能在 stop 返回后直接修改文件。
- 排空是**进程内、已登记入口的屏障**；重启准入见下节。不是身份校验、跨进程锁或完整磁盘恢复事务，只能由宿主协调器在目标用户工作之外调用。
  已登记工作内部等待自身的 `quiesce()` 会自等，不能把这个方法直接做成在当前任务内等待完成的模型工具。
- 直接记忆服务、独立 `runButlerMaintenanceOnce` 调用、spaceUpkeep/retention、会话交付、暂停任务存储及已发出的流内容未被覆盖。
  后续须清理 Git 历史、投影与会话并核验完成，再允许新代际启动；目前不提供 reopen 或生产调用入口。
- 没有扩大上下文、增加 LLM 调用或新环境变量。只使用合成数据，不对真实用户执行隔离或删除。

没有活实例且本进程从未登记的历史用户缓存，仍须由后续持久化协调器独立清理，不能仅遍历现存 router。

## M2 持久化准入隔离

`FileButlerUserIsolation` 在 `<memoryRoot>/.user-isolation/<sha256(userId)>` 建立空目录标记，
与 `user/<userId>` 的记忆文件、用户 Git 树及文件事务标记分离。标记不保存原话、纠正内容或原样用户 ID；
哈希只用于稳定寻址，不是匿名化或加密。目录存在就关闭，未知形状也不能被当成开放。

- main 共享登记器、独立创建的 factory/sweeper 默认使用真实存储；裸 router 无根路径，仍需明确注入。
  每次任务/恢复/维护的入场与成功、异常返回前做只读元数据检查，不读取全量记忆，不新增模型调用或上下文预算。
- 准入检查本身也登记在排空计数内；检查完成后、真正调用工作前再次同步确认本地状态，防止异步间隙越过关闭。
  已观察到关闭或读取失败的用户，在该实例内不自动恢复；底层错误不会夹带路径、内容或 cause 返回。
- `quiesce()` 先同步闭门，再建立空标记并同步目录及实际祖先，成功后才等待在途工作、退役资源并清缓存。
  mkdir/同步失败返回固定错误，不运行清理、不撤掉已有标记；重试即使看到已有标记也重新同步。
  写盘尚未成功前不承诺重启仍关闭，但此前不会开始资源清理；不把失败的隔离当作可继续纠正的许可。
- 根与控制目录拒绝静态符号链接/非目录；关闭操作拒绝非空或非法标记。可信配置的祖先允许系统路径别名，
  校验根后沿真实路径同步，兼容 macOS `/var`、`/tmp`。这不是抵抗恶意进程并发替换目录的跨进程锁。
- 重建实例继续拒绝被隔离用户，其他用户可正常使用。标记没有过期、自动删除或 reopen API，
  也不因原始 JSONL 事务完成而撤销。不要通过手工删除标记绕过未完成的清理。

该阶段只恢复“关闭”状态，不恢复完整纠正任务、不证明历史副本已清理，也不能排空其他进程已运行的工作。
直接记忆服务、独立 retention、会话交付和暂停任务仍未接入，完整可信纠正入口继续关闭。

## M1 验证

- personal-memory：596 项通过；personal-butler：418 项通过（含会话窗 43 项）。
- host：3509 项通过，5 项跳过。沙箱内首跑受到本机监听权限限制，允许本机测试端口后重跑通过。
- 全仓类型检查、两个相关包构建、四道框架约束门、记忆写侧与跨店集成门通过。
- 独立审查发现并修复 UTC 降级二次校验丢锚问题；补测从观察、捕获、去重到渲染的完整降级链路。

## M2 来源保真验证

- personal-memory：663 项通过；personal-butler：418 项通过；host：3510 项通过、5 项原有跳过。
- 全仓类型检查、personal-memory / personal-butler 构建、四道框架约束门、写侧与召回门通过。
- 零凭据 demo 通过；原词召回 100% → 100%，同义词召回 0% → 0%，不沿用旧夹具的虚构提升。
- 两项独立审查分别覆盖整理链与原子事实链；修复作用域别名冲突、已有来源冲突，并补齐
  摘要和画像阶段“来源 ID → 原话切片”的关联断言。宿主健康门验证模型确实被调用且报错。
- 所有验证采用合成记忆和确定性模型替身，没有读取生产对话；未做真实模型日期问答评测。
- 整理保真提交：`c6bcb1e3`。本轮均为本地提交，未推送或部署。

## M2 日期词验证

- personal-memory 704、personal-butler 418、host 3511 项通过，host 5 项原有跳过。
  全仓类型检查、相关包构建、四道约束门、记忆写侧/召回门均通过。
- 41 项日期/证据测试覆盖本地午夜、夏令时、闰日、跨年、未知锚点、非法日期、溢出、旧条目不回填、
  来源损坏拒绝、两代整理及后来时钟/进程时区变化。宿主新增真实捕获→整理→新会话提示词验证。
- 独立审查发现并修复“去年今天”“后天性疾病”等片段误识别，复核通过；未声称已覆盖全部自然语言歧义。
- 一次宿主默认并发重跑停滞，已终止，不计为通过；随后以 4 workers 和逐项报告完整重跑通过。
  未定位停滞根因，不据此修改生产逻辑。所有数据均为合成，未调用真实模型或部署。

## M2 纠正底座验证

- 代码提交：`2d660145`，仅本地；仍未启用纠正执行或部署。
- 文件后端 120、personal-memory 753、personal-butler 418、host 3512 项通过；host 5 项原有跳过。
  共 4803 项通过。全仓类型检查、相关构建、框架约束门和记忆写侧/召回/集成门通过。
- 新增 96 项回归：快照 46 项、纠正 45 项、共享证据读取 4 项、宿主 1 项。
- 审查先复现后修复：正常助手-only 记录误拒绝、双证据表示掩盖来源、非法完整性标记、重建条目补字段后超预算，
  以及外部 owner 对象变更造成队列与路径错位。快照测试补齐同配置 missing/empty 指纹对照。
- 一次宿主全跑因测试子进程意外退出而失败，不计为通过；缺失报告的 Web Push 协议测试单跑 9 项通过，
  随后两次宿主全跑通过，最终为 3512/5。退出根因未定位，不修改或宣称修复相关 Web Push 逻辑。
- 仅用合成数据与真实临时文件存储验证；没有执行磁盘纠正，没有读取私人历史，没有真实模型准确率评测或部署。

## M2 文件提交验证

- 本地代码提交：`fb2e9aa4`（文件事务）、`29060ddc`（内部宿主执行器）。未推送、未部署。
- 文件后端 175、personal-memory 753、personal-butler 418、host 3522 项通过，共 4868 项；host 5 项原有跳过。
  全仓类型检查、host 及依赖构建、四道框架约束门、记忆写侧/召回/集成/逐出门通过。
- 新增 65 项回归，包含跨种类文件修改、旧句柄拒绝读写、完整条目预算、故障注入与恢复、无关来源保留、
  跨用户隔离、15 万条完整快照、崩溃残留后二次删除和旧 pending 重现后的漂移拒绝。
- 独立规格与质量审查先复现再修复三类边界：临时后像残留、清理时二次 I/O 故障解除屏障、无界参数展开。
  修复后重新运行全量验证并复核通过；不把修复前通过的测试当作缺陷不存在的证据。
- 仅使用合成数据、真实临时文件和注入故障；未做真实断电测试或真实模型日期问答评测。
  所谓文件纠正通过不是对用户真实记忆执行过，也不是索引/Git/投影/会话已完成硬删除。

## M2 检索退役验证

- 文件 watermark 本地提交 `2e8aaff0`，宿主检索退役 `e40de2c2`；本轮未推送、未部署。
- 文件后端 190、personal-memory 753、personal-butler 418、host 3578 项通过，共 4939 项；host 5 项原有跳过。
  全仓类型检查、host 及依赖构建、四道框架门与记忆写侧/召回/集成/逐出门通过。
- 本轮新增 71 项回归：watermark 15 项、索引退役 46 项、联想网 10 项。覆盖异步刷新/落盘/融合检索退役、
  磁盘缓存及临时文件清理、失败重试、另一用户不变，以及真实文件“旧索引退役→纠正→新索引/联想网”。
- watermark 测试强化后，删除 generation 绑定、只保留 size、额外直接读取 JSONL 三种变异均被抓住。
  联想网审查复现瞬时 guard 错误被误判为普通建网失败，补四类错误的红绿回归后不再回退旧网。
- 索引审查复现 clear 后新读取加入旧刷新而误返回空结果；补六项确定性并发回归，当前代际发布前不结束新读取。
  覆盖连续 clear、多读取合流、二次刷新中退役与落盘排空；专项共 77 项通过后重新运行上述全量验证。
- 最终独立复核无具体缺陷发现；未把与退役重叠、完成前交付的读取误报为完成后旧数据泄漏。
- 未做真实模型日期问答评测或生产数据迁移，不将缓存屏障算作日期回答准确率提升；统一上下文预算仍待 M3。

## M2 用户排空验证

- 本地代码提交 `8c192e67`，未推送、未部署，也未开放用户纠正或隔离入口。
- file 190、personal-memory 753、personal-butler 418、host 3627 项通过，共 4988 项；host 5 项原有跳过。
  新增 49 项回归，最终六文件专项 59 项通过；全仓类型检查、host 依赖构建、框架门与四道记忆门通过。
- 先红后绿覆盖：慢任务/恢复/取消/关闭排空、迟到错误与暂停状态隔离、多 router 同用户、另一用户继续、
  正常关闭引用释放、失败重试、真实维护/Git/无模型投影，以及真实 Pool 停止、直接替换和任务停止自身。
- 复核先定位再修复共享缓存并发删除、旧 router 强引用、shutdown 重试再写缓存和 Pool 移除未调用 shutdown。
  最终独立规格与质量复核无剩余问题；不用修复前的全绿证明后续发现的边界正确。
- 测试均为合成数据与模型替身；没有真实模型准确率评测、生产记忆操作、跨进程恢复或物理介质安全擦除验证。

## M2 持久化隔离验证

- 代码本地提交 `11ba3ee3`，未推送、未部署，也未启用用户纠正或解封入口。
- file 190、personal-memory 753、personal-butler 418、host 3674 项通过，共 5035 项；host 5 项原有跳过。
  本轮新增 47 项，最终六文件专项 90 项通过；全仓类型检查、host 依赖构建、框架及四道记忆门通过。
- 覆盖持久标记重读、另一用户正常、默认 factory/sweeper 重建后拒绝、文件同步故障与重试、非法目录/符号链接、
  Unicode 身份不混淆、只读准入、迟到结果/异常/暂停状态和准入等待排空。检查完成到真正启动工作的微任务竞态先红后绿。
- 首次宿主全跑两项既有转派测试失败：其两轮 0ms 定时等待不保证文件异步写入完成；该模块未调用新隔离代码。
  单跑 16 项通过，避开同时构建后宿主完整重跑通过。未修改该模块，也不声称已修复这组测试的时序不稳定性。
- 两次独立规格/质量审查无剩余具体问题。验证使用合成目录与故障注入，不包含真实断电实验、
  真实用户记忆操作、真实模型准确率评测或跨进程已运行任务排空；完整硬删除链仍未开放。

设计与逐项执行记录见
[`设计`](../superpowers/specs/2026-09-11-atong-temporal-memory-design.md) 与
[`实施计划`](../superpowers/plans/2026-09-11-atong-temporal-memory.md)。
本轮执行记录见 [`M2 来源保真计划`](../superpowers/plans/2026-09-12-atong-evidence-consolidation.md)。
日期词阶段见 [`M2 日期词计划`](../superpowers/plans/2026-09-12-atong-calendar-references.md)。
纠正底座见 [`M2 纠正底座计划`](../superpowers/plans/2026-09-12-atong-correction-foundation.md)。
文件提交见 [`M2 文件提交计划`](../superpowers/plans/2026-09-12-atong-correction-file-commit.md)。
检索退役见 [`M2 检索退役计划`](../superpowers/plans/2026-09-12-atong-recall-retirement.md)。
用户排空见 [`M2 用户排空计划`](../superpowers/plans/2026-09-12-atong-user-quiescence.md)。
持久化隔离见 [`M2 持久化隔离计划`](../superpowers/plans/2026-09-12-atong-durable-isolation.md)。
