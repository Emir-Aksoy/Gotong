# Grafana datasource provisioning — points the AipeHub dashboard at the
# Prometheus service in this compose stack. Auto-loaded on Grafana startup.
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
