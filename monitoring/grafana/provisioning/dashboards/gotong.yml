# Grafana dashboard provisioning — auto-loads the Gotong overview dashboard
# from /var/lib/grafana/dashboards (the compose stack mounts gotong-overview.json
# there). No manual "Import → upload JSON" step needed.
apiVersion: 1
providers:
  - name: Gotong
    type: file
    disableDeletion: false
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
