{
  "name": "@gotong/example-codex-deepseek-hub",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "Personal-hub case — one router LLM manages Codex + a DeepSeek-backed TUI coding agent as hub Participants, dispatching the right coder per goal; both operate on the SAME repo (shared cwd) so they share AGENTS.md + PROGRESS.md. Deterministic, no API key.",
  "scripts": {
    "start": "tsx src/index.ts",
    "start:real": "tsx src/index.real.ts",
    "start:cli": "tsx src/cli.ts",
    "load-template": "tsx src/load-template.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@gotong/cli-agent": "workspace:*",
    "@gotong/core": "workspace:*",
    "@gotong/llm": "workspace:*",
    "@gotong/llm-openai": "workspace:*",
    "yaml": "^2.6.0"
  }
}
