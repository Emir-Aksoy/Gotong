{
  "name": "@gotong/example-services-sidecar-demo",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "Protocol v1.1 demo — agents connect over WebSocket and call Hub Services (memory) via SERVICE_CALL. Mock provider, no API key. Companion to docs/services-over-ws-rfc.md §9.",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@gotong/core": "workspace:*",
    "@gotong/host": "workspace:*",
    "@gotong/llm": "workspace:*",
    "@gotong/sdk-node": "workspace:*",
    "@gotong/transport-ws": "workspace:*"
  }
}
