{
  "name": "@aipehub/example-personal-coding-hub",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "Personal-hub case — one router LLM actively manages Claude Code + Codex as hub Participants, dispatching coding tasks across them; both operate on the SAME repo (shared cwd) so they share project-level files (AGENTS.md spec + PROGRESS.md handoff log). Deterministic, no API key.",
  "scripts": {
    "start": "tsx src/index.ts",
    "start:real": "tsx src/index.real.ts",
    "start:cli": "tsx src/cli.ts",
    "load-template": "tsx src/load-template.ts",
    "consult-core": "tsx src/consult.selfcheck.ts",
    "consult": "tsx src/consult-demo.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@aipehub/cli-agent": "workspace:*",
    "@aipehub/core": "workspace:*",
    "@aipehub/llm": "workspace:*",
    "@aipehub/llm-openai": "workspace:*",
    "yaml": "^2.6.0"
  }
}
