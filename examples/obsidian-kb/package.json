{
  "name": "@aipehub/example-obsidian-kb",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "Obsidian vault as a knowledge base — an agent that reads/searches your Obsidian notes via the mcp-obsidian MCP server (Local REST API plugin). AipeHub never touches the vault; the MCP server does.",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@aipehub/core": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.7.0"
  }
}
