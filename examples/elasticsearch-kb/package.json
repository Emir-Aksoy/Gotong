{
  "name": "@gotong/example-elasticsearch-kb",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "Elasticsearch index as a knowledge base — an agent that searches your ES indices via the official @elastic/mcp-server-elasticsearch MCP server. Gotong never touches the cluster; the MCP server does.",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@gotong/core": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.7.0"
  }
}
