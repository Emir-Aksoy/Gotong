{
  "name": "@gotong/example-loadtest",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "Pre-launch load-test harness — measures dispatch throughput, latency percentiles, and memory growth for a Hub under N parallel agents.",
  "scripts": {
    "inproc": "tsx src/inproc.ts",
    "ws": "tsx src/ws.ts",
    "report": "tsx src/report.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@gotong/core": "workspace:*",
    "@gotong/sdk-node": "workspace:*",
    "@gotong/transport-ws": "workspace:*"
  }
}
