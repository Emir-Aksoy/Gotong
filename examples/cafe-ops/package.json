{
  "name": "@gotong/example-cafe-ops",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "门店运营 (奶茶 / 咖啡店) — an ORGANIZATION hub template: declarative workflows (staff onboarding / shift availability / overtime claim) with surface.me member self-service and human: manager approval (HITL). Deterministic, no API key.",
  "scripts": {
    "start": "tsx src/index.ts",
    "load-template": "tsx src/load-template.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@gotong/core": "workspace:*",
    "@gotong/workflow": "workspace:*",
    "@gotong/inbox": "workspace:*",
    "yaml": "^2.6.0"
  }
}
