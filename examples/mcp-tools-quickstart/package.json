{
  "name": "@gotong/example-mcp-tools-quickstart",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "Stand-alone demo of @gotong/mcp-client — spawn the filesystem MCP server, list its tools, call one. No Hub, no LLM, just the toolset.",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@gotong/mcp-client": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.23.1"
  }
}
