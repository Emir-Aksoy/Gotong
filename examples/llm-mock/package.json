{
  "name": "@gotong/example-llm-mock",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "LlmAgent + MockLlmProvider — runs without any API key, exercises the LLM agent pipeline end-to-end.",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@gotong/core": "workspace:*",
    "@gotong/llm": "workspace:*"
  }
}
