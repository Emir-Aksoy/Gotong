{
  "name": "@gotong/example-open-space",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "Open Space demo (v1.1) — Hub with admin-approval gating, web admin/worker UIs, and a remote writer agent that needs admin approval to join.",
  "scripts": {
    "start": "tsx src/launcher.ts",
    "host": "tsx src/host.ts",
    "agent": "tsx src/agent.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@gotong/core": "workspace:*",
    "@gotong/transport-ws": "workspace:*",
    "@gotong/sdk-node": "workspace:*",
    "@gotong/web": "workspace:*"
  }
}
