{
  "name": "@gotong/example-cli-human",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "Terminal as a human-in-the-loop adapter — read tasks from stdout, respond via stdin.",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@gotong/core": "workspace:*",
    "@gotong/llm": "workspace:*"
  }
}
