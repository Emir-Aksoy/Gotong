{
  "name": "@aipehub/example-cli-human",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "Terminal as a human-in-the-loop adapter — read tasks from stdout, respond via stdin.",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@aipehub/core": "workspace:*",
    "@aipehub/llm": "workspace:*"
  }
}
